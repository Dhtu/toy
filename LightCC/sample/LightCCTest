def average(x,y) (x + y) * 0.5;

extern sin(x);
extern cos(y);
def sscc(x,y) sin(x)*sin(x)+cos(y)*cos(y);

def fib(x)
  if x < 3 then
    1
  else
    fib(x-1)+fib(x-2);

def unary!(v)
  if v then
    0
  else
    1;

extern printd(x);
def Main()
    printd(!0);